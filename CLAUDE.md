# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**infinite-jest** is an autonomous agent experiment: an agent suggests its next action, then the next execution performs that action — no rails, maximum context retention. Designed for use with local LLMs to avoid burning tokens on paid APIs.

## Development

No build system or test infrastructure exists yet. Update this file as the project takes shape.
